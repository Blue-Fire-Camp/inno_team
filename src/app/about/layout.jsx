export const metadata = {
  title: "About Us",
  description: "Learn about SuperValue LLC — a California-registered IT consulting and talent sourcing firm built by an engineering-led remote team.",
};

export default function AboutLayout({ children }) {
  return children;
}
